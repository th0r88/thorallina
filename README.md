# Thorallina d.o.o. — Landing Page

Company landing page hosted on GitHub Pages.

## Structure

Single file: `index.html` — no build step, no dependencies.

## Deploy

Hosted via GitHub Pages. Any push to `main` updates the live site.
